aiohttp
beautifulsoup4
requests
# influxdb-client is only needed when [main].backend = "influx".
# It is imported lazily, so VictoriaMetrics-only deployments can omit it.
# Replace with influxdb-client[ciso] for better performance (larger image).
influxdb-client
PyJWT==2.3.0
